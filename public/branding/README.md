# Branding Assets

Current app logos used by `BrandLogo`:

1. `mytrainingplan-logo-full.png`
- Used for landing hero and wordmark contexts.
- Recommended transparent background.
- Current usage width target in app: up to ~520px (`size=landing`).

2. `mytrainingplan-logo-mark.png`
- Used for header/sidebar mark and compact app branding.
- Rendered as cropped square mark via `object-fit: cover`.
- Recommended square image, transparent background.

Implementation reference:
- `src/components/BrandLogo.tsx`
- `src/components/BrandLogo.module.css`
