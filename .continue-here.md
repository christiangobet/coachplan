---
status: in_progress
last_updated: 2026-03-01T00:00:00Z
---

<current_state>
Mid-implementation: personal vs public templates feature.
Only step 1 of 6 is done. Pausing due to context limit.
</current_state>

<completed_work>

- Strava compliance: connect button 48px, View on Strava links (commit b8872c2)
- Strava review: force-dynamic, no-cache, correct providerActivityId for links (commit f397c50)
- Step 1 DONE: save-as-template default isPublic changed false→true in src/app/api/plans/[id]/save-as-template/route.ts line 29

</completed_work>

<remaining_work>

## Personal vs Public Templates — Steps 2–6

### Step 2: PATCH /api/plans/[id]/route.ts — add isPublic field
File: src/app/api/plans/[id]/route.ts

In the PATCH handler, the `updates` object type (around line 87) needs `isPublic?: boolean` added.
The "no supported fields" guard (line 142) needs `'isPublic'` added to the check.
Add this block after the planSummary block (after line 140):

```ts
if ('isPublic' in body) {
  if (typeof body.isPublic !== 'boolean') {
    return NextResponse.json({ error: 'isPublic must be a boolean' }, { status: 400 });
  }
  // Only the template owner can change visibility
  if (existingPlan.ownerId !== user.id) {
    return NextResponse.json({ error: 'Only the template owner can change visibility' }, { status: 403 });
  }
  updates.isPublic = body.isPublic;
}
```

Also add `isPublic?: boolean` to the `updates` type definition on line 87.
And add `|| 'isPublic' in body` to the "no supported fields" check on line 142.

### Step 3: GET /api/plans/route.ts — return myTemplates alongside plans
File: src/app/api/plans/route.ts

In the GET handler (around line 1896), after the plans query, add:

```ts
const myTemplates = await prisma.trainingPlan.findMany({
  where: { ownerId: user.id, isTemplate: true },
  orderBy: { createdAt: 'desc' },
  select: {
    id: true,
    name: true,
    weekCount: true,
    isPublic: true,
    planGuide: true,
    planSummary: true,
    createdAt: true,
  }
});
```

Change the return from:
```ts
return NextResponse.json({ plans: plansWithProgress });
```
to:
```ts
return NextResponse.json({ plans: plansWithProgress, myTemplates });
```

### Step 4: from-template/route.ts — access guard for private templates
File: src/app/api/plans/from-template/route.ts

After the `if (!template)` check (line 36), add:

```ts
// Allow access if template is public OR if current user is the owner
if (!template.isPublic && template.ownerId !== user.id) {
  return NextResponse.json({ error: 'Template not found' }, { status: 404 });
}
```

### Step 5: PlansClient.tsx — full UI rewrite of template sections
File: src/app/plans/PlansClient.tsx

KEY CHANGES:
a) Add MyTemplate type:
```ts
type MyTemplate = { id: string; name: string; weekCount?: number | null; isPublic: boolean; planGuide?: string | null; planSummary?: PlanSummary | null };
```

b) Replace `const [templates, setTemplates] = useState<Template[]>([]);` with:
```ts
const [myTemplates, setMyTemplates] = useState<MyTemplate[]>([]);
const [publicTemplates, setPublicTemplates] = useState<Template[]>([]);
const [togglingVisibilityId, setTogglingVisibilityId] = useState<string | null>(null);
```

c) In the load() useEffect, change plans response handling to also extract myTemplates:
```ts
if (plansRes.ok) {
  const plansData = await plansRes.json().catch(() => null);
  if (!cancelled) {
    setPlans(Array.isArray(plansData?.plans) ? plansData.plans : []);
    setMyTemplates(Array.isArray(plansData?.myTemplates) ? plansData.myTemplates : []);
  }
}
```
And change templates response to use setPublicTemplates:
```ts
if (templatesRes.ok) {
  const templatesData = await templatesRes.json().catch(() => null);
  if (!cancelled) {
    setPublicTemplates(Array.isArray(templatesData?.templates) ? templatesData.templates : []);
  }
}
```

d) In handleSaveAsTemplate, after save, re-fetch /api/plans to get updated myTemplates:
```ts
const pRes = await fetch('/api/plans');
if (pRes.ok) {
  const pData = await pRes.json().catch(() => null);
  setPlans(Array.isArray(pData?.plans) ? pData.plans : []);
  setMyTemplates(Array.isArray(pData?.myTemplates) ? pData.myTemplates : []);
}
```
(Remove the existing /api/templates re-fetch from handleSaveAsTemplate)

e) Add handleToggleVisibility:
```ts
const handleToggleVisibility = async (tplId: string, currentIsPublic: boolean) => {
  setTogglingVisibilityId(tplId);
  try {
    const res = await fetch(`/api/plans/${tplId}`, {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ isPublic: !currentIsPublic })
    });
    if (!res.ok) return;
    setMyTemplates((prev) => prev.map((t) => t.id === tplId ? { ...t, isPublic: !currentIsPublic } : t));
    // If made public, refresh public list
    if (!currentIsPublic) {
      const tRes = await fetch('/api/templates');
      if (tRes.ok) {
        const tData = await tRes.json().catch(() => null);
        setPublicTemplates(Array.isArray(tData?.templates) ? tData.templates : []);
      }
    } else {
      // Made private: remove from public list
      setPublicTemplates((prev) => prev.filter((t) => t.id !== tplId));
    }
  } finally {
    setTogglingVisibilityId(null);
  }
};
```

f) Fix handleRenameTemplate to use myTemplates:
  - `setTemplates(...)` → `setMyTemplates(...)`

g) Fix deletePlan: already removes from both `plans` and `templates` — change `templates` to `myTemplates`:
  - `setTemplates((prev) => ...)` → `setMyTemplates((prev) => ...)`

h) Replace the single "Templates" section (lines 702–835) with two sections:

**"My Templates" section** (own templates, private or public):
```tsx
{(myTemplates.length > 0) && (
  <section className="plans-section plans-shell-section">
    <h2 className="plans-section-title">My Templates</h2>
    <div className="plans-grid">
      {myTemplates.map((tpl) => (
        <div className="plan-card template" key={tpl.id}>
          <div className="plan-card-top">
            <span className={`plan-template-badge ${tpl.isPublic ? 'public' : 'personal'}`}>
              {tpl.isPublic ? 'Public' : 'Personal'}
            </span>
            <button className="plan-card-menu-btn" onClick={() => toggleMenu(tpl.id)} aria-label="More actions">···</button>
          </div>
          {renamingTemplateId === tpl.id ? (
            /* rename inline form — same as existing */
          ) : (
            <h3 className="plan-card-name">{tpl.name}</h3>
          )}
          <span className="plan-card-meta">{tpl.weekCount ? `${tpl.weekCount} wks` : 'No weeks set'}</span>
          {/* guide preview — same as existing */}
          {/* use template form — same as existing */}
          {expandedMenuId === tpl.id && (
            <div className="plan-card-overflow-menu">
              <button className="plan-card-overflow-item" onClick={() => { setRenameValue(tpl.name); setRenamingTemplateId(tpl.id); setExpandedMenuId(null); }}>Rename</button>
              <button
                className="plan-card-overflow-item"
                onClick={() => { handleToggleVisibility(tpl.id, tpl.isPublic); setExpandedMenuId(null); }}
                disabled={togglingVisibilityId === tpl.id}
              >
                {togglingVisibilityId === tpl.id ? 'Saving…' : tpl.isPublic ? 'Make private' : 'Make public'}
              </button>
              {(tpl.planSummary || tpl.planGuide) && (
                <button className="plan-card-overflow-item" onClick={() => { setExpandedGuideId((prev) => (prev === tpl.id ? null : tpl.id)); setExpandedMenuId(null); }}>
                  {expandedGuideId === tpl.id ? 'Hide preview' : 'Show preview'}
                </button>
              )}
              <button className="plan-card-overflow-item danger" onClick={() => { deletePlan(tpl.id); setExpandedMenuId(null); }} disabled={processingPlanId === tpl.id}>
                {processingPlanId === tpl.id ? 'Deleting…' : 'Delete'}
              </button>
            </div>
          )}
        </div>
      ))}
    </div>
  </section>
)}
```

**"Public Templates" section** (community templates, owned by anyone):
- Filter out templates the user owns (they see those in My Templates): `publicTemplates.filter(t => !myTemplates.some(m => m.id === t.id))`
- Same card UI as existing template cards (no rename/delete — read-only for other people's templates)
- Rename section title from "Templates" to "Public Templates"

### Step 6: plans.css — visibility badges
File: src/app/plans/plans.css

Add after existing `.plan-template-badge` styles:
```css
.plan-template-badge.personal {
  background: rgba(107, 114, 128, 0.12);
  color: var(--d-text-mid);
}

.plan-template-badge.public {
  background: rgba(15, 138, 71, 0.12);
  color: #0f8a47;
}
```

</remaining_work>

<decisions_made>
- Personal templates default to isPublic: false (changed from true)
- Own templates appear in "My Templates" section; community templates in "Public Templates"
- Visibility toggle via PATCH /api/plans/[id] with ownership guard
- GET /api/plans returns { plans, myTemplates } — myTemplates are athlete's own templates
- from-template allows private template use only by the owner
</decisions_made>

<context>
No schema migration needed — isPublic and ownerId already exist.
Only 1 of 6 steps done (save-as-template default). Steps 2-6 remain.
</context>

<next_action>
/clear → implement steps 2–6 by reading .continue-here.md and executing each change.
</next_action>
